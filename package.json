{
  "name": "upgrade-yarn-workspaces-deps",
  "version": "2.0.2",
  "bin": {
    "upgrade-yarn-workspace-deps": "./index.js"
  },
  "license": "MIT",
  "description": "Small tool to upgrade dependencies across many yarn workspaces at once.",
  "keywords": [
    "yarn",
    "cli",
    "dependencies"
  ],
  "repository": {
    "type": "git",
    "url": "https://github.com/NickHeiner/upgrade-yarn-workspaces-deps.git"
  },
  "scripts": {
    "test": "lint",
    "lint:fix": "eslint --cache --fix index.js"
  },
  "dependencies": {
    "load-json-file": "^6.2.0",
    "lodash": "^4.17.15",
    "yargs": "^15.3.1"
  },
  "eslintConfig": {
    "env": {
      "es6": true,
      "node": true
    },
    "rules": {
      "no-console": "off"
    },
    "extends": "nth"
  },
  "devDependencies": {
    "eslint": "^6.8.0",
    "eslint-config-nth": "^1.0.2"
  }
}
